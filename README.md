# Process-Memory-Inspector
 A Linux C tool that inspects live process memory by reading the /proc filesystem — maps and classifies stack, heap, text, and data segments in real time with no external libraries.
